{
  "name": "autoskip",
  "version": "3.1.0",
  "private": true,
  "description": "AutoSkip plugin for Lampa",
  "scripts": {
    "build": "node scripts/build.js",
    "watch": "node scripts/build.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  }
}

